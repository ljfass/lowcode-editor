export enum Position {
  Static = 'static',
  Relative = 'relative',
  Absolute = 'absolute',
  Fixed = 'fixed',
  Sticky = 'sticky',
}

export enum Float {
  None = 'none',
  Left = 'left',
  Right = 'right',
}

export enum ClearFloat {
  None = 'none',
  Left = 'left',
  Right = 'right',
}
