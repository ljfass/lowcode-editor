import { DraggableDetectAreaDirective } from './draggable-detect-area.directive';

describe('DraggableDetectAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableDetectAreaDirective();
    expect(directive).toBeTruthy();
  });
});
