import { MousemoveoutDirective } from './mousemoveout.directive';

describe('MousemoveoutDirective', () => {
  it('should create an instance', () => {
    const directive = new MousemoveoutDirective();
    expect(directive).toBeTruthy();
  });
});
